/**
 * Management of inexor core clients.
 * @module gameclient
 */

const ScreenManager = require('./ScreenManager');
const LayerManager = require('./LayerManager');

module.exports = {
  ScreenManager: ScreenManager,
  LayerManager: LayerManager
}

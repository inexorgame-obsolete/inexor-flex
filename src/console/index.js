/**
 * Management of Inexor Core consoles.
 * @module console
 */

const ConsoleManager = require('./ConsoleManager');
const CommandManager = require('./CommandManager');

module.exports = {
  ConsoleManager: ConsoleManager,
  CommandManager: CommandManager
}
